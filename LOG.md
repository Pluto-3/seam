# Log

Dated entries for things discovered while building — dead ends, surprising behavior, decisions that changed. Not a running diary of everything done; only what's actually worth remembering later. When something here changes a decision, update DESIGN.md too and note it here.

## 2026-07-15

Project started. Design sketch settled: three-tier agent model (crowd/leads/hatch), one event log read three ways (strategy/data/narrative exporters), graph world with continuous rendering on top, seven-action set (move/gather/consume/trade/rest/craft/signal). Reuse of Fahamu/LAUA/Waypoint/Njia is opportunistic — dropped immediately if it constrains the design. No formal spec — phased build instead, starting with a headless Phase 0 to test whether the trade/scarcity mechanic actually produces interesting behavior before spending time on rendering or reasoning-driven agents.

## 2026-07-15 — Phase 0 built and verified

Built all nine Phase 0 modules per the build plan (constants/world/agents/decide/actions/tick/log/stats/run) — Python, stdlib only, no third-party dependencies. Selftest and a small hand-traceable run (3 agents, 4 nodes, 10 ticks) both passed cleanly on the first attempt — the tick order, trade initiator/responder resolution, and diminishing-marginal-value scoring all behaved as designed.

**Bug found — Signal was dead code.** First full run (40 agents, 15 nodes, 3000 ticks, seed 42) showed `signals active: 0` at every single snapshot. Checked the raw log: zero `SIGNAL` entries across the entire run. Root cause: `SIGNAL_VALUE = 0.1` was tiny next to typical Gather/Trade/Rest scores (roughly 1-20), so Signal never won the utility argmax even when feasible. The mechanic existed in code but never actually fired. Fixed by raising `SIGNAL_VALUE` to 4.0, comparable in magnitude to the actions it competes with.

**Regression found — signal-driven routing caused a stuck 2-node oscillation.** Raising `SIGNAL_VALUE` alone wasn't enough to close the loop described in DESIGN.md ("this is what makes stigmergy actually feed back into Move scoring"), so also added `SIGNAL_MOVE_BONUS` as a flat additive term in the Move-scoring lookahead. Result was much worse than the original bug: population collapsed 40→15 by tick 1500 (21 dead vs. 2 in the original healthy run), cumulative trades dropped roughly 10x, crafting stopped entirely after the first few hundred ticks, and one single route (`n10-n3`) was traversed 3000+ times — individual agents were moving on 2200+ of 3000 ticks. Reducing the bonus from 2.5 to 0.4 did not fix it. Isolated the cause by zeroing just the move-bonus term while leaving `SIGNAL_VALUE` alone: health was fully restored (38/40 alive, 6461 cumulative trades, 7305 crafts) **and** signals were still firing on their own merit (54-60 active). Conclusion: a flat additive bonus in this scoring architecture is enough to overturn the intentional `MOVE_LOOKAHEAD_DISCOUNT` that makes moving cost more than staying, creating a resonant back-and-forth between two mutually-attractive neighbors. Left `SIGNAL_MOVE_BONUS = 0.0` for now — signals post and expire correctly and show up in stats, but don't yet influence routing. **Follow-up, not yet done:** wiring signals into Move scoring needs a design that can't be flatly additive — e.g. only applying if it doesn't overturn an already-substantially-better local option, or scaling relative to the local score rather than as a constant.

**Verification results, current tuning (`SIGNAL_MOVE_BONUS = 0`), seed 42, 40 agents / 15 nodes / 3000 ticks:**
- Trade count grows steadily and never flattens (6124-6461 cumulative across runs, ~850-920 per 500-tick window, consistent).
- Specialization index settles around 0.41-0.44 within the first 500 ticks and holds there — not a gradual upward trend as originally hypothesized in the plan, but a fast convergence to a stable, clearly-nonzero level. Still real evidence of trade-driven redistribution, just a different shape than expected.
- **Negative control confirms trade matters**: same seed with `--no-trade` gives specialization index 0.27 vs. 0.42 with trade on — a clean, meaningful gap, not noise.
- Population survival is seed-sensitive: seed 42 kept 38/40 agents alive through 3000 ticks, but seeds 7 and 99 lost roughly half the population (21/40, 19/40). Trade and specialization still emerge robustly in both (2152 and 4982 cumulative trades, 0.48 and 0.54 specialization index — both well above the no-trade baseline), so the core mechanic holds, but metabolism/gather/consume rate balance needs more tuning before population sustainability can be called solid. Also saw food hoarding to extreme levels in seed 99 (avg 502 units/agent) — Consume competing poorly against Gather once hunger is low enough, nothing currently bounds accumulation.

**Bottom line on the Phase 0 question ("does scarcity + specialization produce visible trade and route behavior"): yes, confirmed** — via hand-trace, a clean negative control, and consistency across three seeds. Two real bugs were caught and one fixed cleanly (dead Signal action), one reverted after diagnosis rather than shipped half-working (Move-bonus oscillation). Two genuine tuning follow-ups are open: population sustainability varies by seed, and food accumulation is currently unbounded. Neither blocks moving toward Phase 1 — they're constants.py tuning work, not architecture problems.

## 2026-07-15 — Phase 1 built: live rendering

Built `layout.py` (circular node layout, pure function, no pygame import), `render.py` (drawing: edges, nodes colored by resource type and sized by current quantity, agents as colored dots, a HUD line), and `watch.py` (the live loop: pygame window, tick-rate-paced simulation calling the unmodified `tick.run_tick`, exponential-smoothing agent movement between nodes, pause/speed/quit controls). Added `pygame` as the project's first external dependency, in a local `.venv` matching the convention already used by the sibling `fahamu` project. Added one small public accessor (`StatsTracker.cumulative_trades`) rather than reaching into a private attribute from `watch.py` — the only touch to a Phase 0 file, purely additive, no existing behavior changed.

**What I could verify myself:** an automated smoke test (`SDL_VIDEODRIVER=dummy`, no real window) ran the full tick loop and every draw call for 8 seconds at 15 ticks/sec without errors or exceptions. Then launched it for real against the actual display (`DISPLAY=:1` — this machine has a genuine X11 desktop session, confirmed before choosing pygame over a browser-based approach) and confirmed the process stays alive and error-free over an extended run, seed 42, same seed already analyzed headlessly.

**What I could not verify myself, by nature of the task:** whether the circular layout is actually legible, whether the movement smoothing feels right, whether node sizing/coloring reads clearly at a glance — Phase 1's actual success bar ("is it watchable") is a subjective, visual judgment call that needs a human looking at the screen, not something a smoke test can confirm. Left the live window running (seed 42, 40 agents, 15 nodes, 6 ticks/sec, controls: space to pause, +/- for speed, esc to quit) for that review. Whatever's actually observed — good, bad, needs a different layout entirely — is worth its own dated entry here.

User's verdict: "alright" — acceptable, not glowing. Watchable, no specific complaints raised. Left as-is; revisit if a real complaint surfaces later rather than polishing blind.

## 2026-07-15 — Phase 0 follow-ups: population collapse root-caused and mostly fixed

Went back to the two open Phase 0 items before starting Phase 2, on the reasoning that building leads/player on top of an unstable crowd tier would make future bugs harder to attribute. Diagnosed via direct JSONL log analysis (death entries + the 6 ticks before each), not the live view — precise trajectory data beats eyeballing pygame for root-causing a specific death.

**Finding 1 — real bug: fractional food was permanently stranded.** `Consume` required `held >= 1.0` to even be considered. An agent gathering an off-specialty resource (0.5x multiplier) or gathering from a nearly-depleted node could easily end up holding e.g. `food: 0.19` — food it was carrying but could never eat, because the threshold was all-or-nothing. Fixed: `Consume` now fires on any positive amount, with hunger relief scaled proportionally (`decide.py`'s Consume candidate, `actions.py`'s `resolve_consume`).

**Finding 2 — real bug: unbounded gather with nothing to spend it on.** Traced the food-hoarding seed (99, avg 502 food/agent) to `Gather`'s score decaying toward zero as held amount grows, but never reaching it, while `Rest`/`Consume` genuinely hit zero once needs are met — so `Gather` kept winning by default against idle alternatives, adding a full batch each time. Fixed: added `MAX_USEFUL_HOLDING` (25 units) — an agent stops generating a `Gather` candidate for a resource once holding that much, in both the direct candidate (`decide.py`) and the Move lookahead (`_best_local_score`).

**Finding 3 — real bug, the dominant one: agents can be topologically blind to food.** Traced several seed-99 deaths to agents with `food: 0.0` busily crafting/trading ore and wood right up until hunger hit exactly 100. Root cause, confirmed by inspecting the generated graph directly: node `n2` (and n8, n9, n12, n14) had **zero food nodes within 1 hop** — the Move-scoring lookahead is 1-hop only, so an agent stationed there structurally could not "see" any reason to travel toward food, no matter how hungry. Fixed with a targeted escape valve, not a general deeper lookahead: once hunger crosses `HUNGER_EMERGENCY_THRESHOLD` (60) with no food in hand, BFS (unweighted, `_bfs_next_hop_to_food` in `decide.py`) finds the nearest node with food currently available and biases Move toward the first hop of that path, scaled by hunger pressure so it ramps in rather than snapping on. Since `decide()` re-runs fresh every tick, this self-corrects hop by hop into de facto multi-hop pathfinding for the one case that actually needs it, without paying lookahead cost on every decision.

**Verification, seeds 7 / 42 / 99, 40 agents / 15 nodes / 3000 ticks, before -> after all three fixes:**
- Seed 99 (the worst case): 19/40 -> **40/40** alive. Zero deaths.
- Seed 7: 21/40 -> 24/40 alive.
- Seed 42 (already healthy): unchanged at 38/40.
- Trade and craft counts rose substantially in all three (more surviving agents means more economic activity) and specialization index held steady at 0.41-0.49 — still clearly above the 0.27 no-trade baseline, so the core Phase 0 finding is intact, not just population that improved.

**Seed 7's remaining 16 deaths are a different, more legitimate phenomenon, not a bug**, worth naming rather than chasing further right now: re-diagnosed after the fixes and found the dying agents standing *at* the food node itself, `Gather` failing 6 ticks in a row before death. This is congestion, not blindness — enough agents compete for the same food node in the same tick that regen (1-5/unit) can't keep up, and whoever loses the shuffled resolution order that tick gets nothing, repeatedly. That's a real tragedy-of-the-commons dynamic a resource economy can legitimately produce, not stranded/misrouted food. Left alone for now — a fix here (e.g. some notion of queueing or reduced contention) is a real follow-up but a different kind of change than the three bugs above, worth its own consideration rather than bolting on quickly.

## 2026-07-15 — Phase 1 interface: legend, live specialization index, emergency cue, live tuning

User's ask, after seeing the first Phase 1 build: make it "reflect what's already built" and be "more understandable," and asked directly whether live-tweaking constants while the sim runs is hard. Talked it through before touching code, per the user's request.

**On the tweaking question**: turned out not to be hard at all — every module reads constants as `C.NAME` at the point of use rather than freezing a copy at import time, so mutating the `constants` module's attributes from `watch.py` while it's running takes effect on the very next tick everywhere, with zero architecture changes needed. The actual design work was picking a small curated list worth exposing (most of the ~35 constants don't change anything visible) and how to interact with them without pygame having real GUI widgets — went with reusing the existing keyboard-control pattern (`tab` cycles a constant, `up`/`down` nudges it ±10%) rather than building sliders. Deliberately transient: nothing writes back to `constants.py` on disk, so watching a live run can never quietly change the checked-in defaults.

**Built:** a legend (`render.py`'s `draw_legend`) mapping node/agent colors to resource types, since none of that was previously labeled; the specialization index now shown live on the HUD (it was computed since Phase 0 but was invisible in the live view even though it's the single number that actually proves trade is working); an explicit death count; a red ring around any agent currently in food-emergency BFS-routing mode (`render.py`'s `is_food_emergency`, mirroring `decide.py`'s own trigger condition from public state, not shared internals) so last session's routing fix is now actually visible on screen instead of indistinguishable from normal movement; and the curated live-tuning controls in `watch.py` (`SPECIALTY_GATHER_MULTIPLIER`, `HUNGER_RATE`, `CONSUME_HUNGER_RELIEF`, `MAX_USEFUL_HOLDING`, `TOOL_DURABILITY`, `SIGNAL_MOVE_BONUS`).

**Bug caught before it shipped**: a purely multiplicative ±10% step can never move a value off exactly zero — and `SIGNAL_MOVE_BONUS` currently *is* exactly zero (deliberately, from the earlier oscillation bug). Tested `adjust_tunable` directly against all six curated constants before trusting it in the live loop and caught this immediately (`0.0 * 1.1 = 0.0`). Fixed with a small `ZERO_STEP` (0.5) that an increase-from-zero jumps to instead, after which it behaves multiplicatively like everything else. This is deliberately handy: it means someone can now use the live view itself to re-explore the `SIGNAL_MOVE_BONUS` space we backed away from, informed by the tuning history already in this log, without editing code.

**Follow-up same day: HUD text was overlapping the graph, then the legend.** First pass only reserved a uniform margin, so nodes near the top of the circular layout landed behind the HUD text. Fixed with an asymmetric top margin. User reported it was *still* cramped and that the HUD and legend now ran into each other — rather than guess again, measured actual rendered widths with `pygame.font.size()`: the specialization-index line was 1050px and the tunable line was 800px, both wider than the entire 900px window, guaranteed to run through the legend column regardless of margins. Fixed properly this time: split those two lines into shorter ones (each verified under a 470px column budget), widened the window to 1300x950, and re-measured before calling it done rather than eyeballing it. Lesson worth keeping: for text layout, measure with the actual font, don't estimate by character count.

## 2026-07-15 — Phase 2 stage 1: leads work, and the fallback design paid off immediately

Checked before building anything: Ollama was already running locally with several models pulled, so no API key question at all. Went with `llama3.2:3b`.

Key design decision, made in planning and confirmed correct by the actual result: leads never generate an action from scratch, they pick a number from a short list of the exact same legal candidates `decide.py` already computes for crowd agents. This meant `decide.py`'s own scoring/legality logic needed zero duplication — just made `_candidates` public as `generate_candidates` so `leads.py` could call it directly (renaming avoided a name collision with the `candidates` local variable already used in `choose_action`).

**Verified in two parts, per the plan, before touching any pygame code:**
1. `test_leads.py` (standalone, headless) ran 2 leads with different goals for 400 ticks against the real local model. Result: **40/40 decisions were genuine LLM choices, zero fallbacks needed.** More importantly, the behavior is actually goal-differentiated, not just "doing stuff": `lead0` ("become the wealthiest trader") chose `TRADE` on nearly every single decision. `lead1` ("keep this settlement stocked on wood") showed a broader gather/rest/trade/consume mix consistent with settlement management rather than pure profit-seeking. Neither is scripted to behave that way — it falls directly out of the goal text in the prompt.
2. Re-ran the same test with a deliberately nonexistent model name (`--break-model`) to prove the fallback path isn't just theoretical: **10/10 decisions correctly fell back to argmax**, and both leads kept behaving sensibly (crafting, trading, consuming) throughout — a broken LLM backend degrades a lead to "just a crowd agent," not a crash or a frozen agent.

Nothing surprising or wrong turned up at this stage - worth recording that explicitly, since so far every phase has surfaced at least one real bug. This one didn't, which is itself useful signal that the "pick from real options, argmax as fallback" design was the right call going in, not just a safety net that happened to be unnecessary.

## 2026-07-15 — Phase 2 stage 2: standing orders work, but my first verification metric was wrong

Added `_order_multiplier()` in `decide.py`: a lead posting an `order:<resource>` signal (same Signal action and mechanism crowd agents already use for `scarce:`/`rich:`, just a different kind string) multiplies Gather's score at that node for any crowd agent. First pass only applied it to the direct Gather candidate. Plan's own verification step called for measuring "wood gathered at the node, with vs. without an order" (`test_orders.py`) - first run came back essentially flat (0.97x, if anything slightly down), which would mean the feature does nothing.

Didn't accept that at face value. Checked the multiplier function in isolation first (correct: 1.6x with the signal present, 1.0x without) - so the bug wasn't in the multiplier itself. Traced it to scope: the multiplier only affected agents already standing at the node: nothing pulled *new* agents there, because the 1-hop Move lookahead (`_best_local_score`) never looked at order signals at all. Fixed by applying the same `_order_multiplier` inside `_best_local_score`, so a neighbor with an active order actually looks more attractive to travel toward, not just to gather at once you're already there.

**Re-ran with the fix — still flat (1.04x) on the original metric.** Didn't stop there either: added instrumentation for gather *attempts* (success + failure), not just successful volume, and found the real story: attempts at the node jumped from 300 (0 failures) to 1840 (659 succeeded, 1181 failed) with the order active - a 6x increase in crowd attention. But total wood *extracted* barely moved, because that node's output is bottlenecked by its regen rate (3.71/tick), not by how many agents want to gather there - it was already supply-constrained before the order existed. More demand under a supply constraint just produces more contention and smaller shares per success (same dynamic as the seed-7 gather-congestion finding from Phase 0), not more total output.

**The actual lesson, worth keeping:** "total resource extracted" is the wrong metric to prove a demand-side mechanism is working whenever the node in question is regen-bound rather than demand-bound - it can look completely flat even when the mechanism is working exactly as designed. Rewrote `test_orders.py` to report attempt counts (the real signal of crowd attention shifting) alongside volume, not just volume. Final result: **6.13x more gather attempts at the ordered node** - standing orders are confirmed to genuinely shape crowd behavior, not cosmetic, matching this phase's stated success bar.

## 2026-07-15 — Phase 2 stage 3: leads and the hatch wired into the live view

Stage 3 turned out to need more than just the player hatch: `watch.py` had zero lead integration up to this point (leads only existed in the headless `test_leads.py`), and Phase 2's own success bar requires a lead's goal to be *visibly* shaping things - so this stage wired both leads and the player into the live loop together, not just the hatch alone.

`watch.py` now spawns `leads.spawn_leads(2, ...)` alongside the crowd and a dedicated player `AgentState` (`tier="lead"`, `id="player"`). Every tick, leads due for a fresh decision (per `leads.LEAD_DECISION_INTERVAL`) and the possessed player (if any) both feed into the same `external_intents` dict tick.py already supports - no new plumbing needed there, exactly as planned.

The hatch: `p` toggles possession. While possessed, number keys move toward a numbered neighbor (drawn live on screen next to each edge via `render.draw_player_moves`, since the graph's neighbor set changes every time the player moves), with dedicated keys for gather/consume/craft/rest/trade/standing-order. Trade and standing-order aren't simple `Intent()` construction - trade auto-picks the best available partner via `generate_candidates` (the same function leads use), and standing-order posts an `order:<resource>` signal, directly exercising the stage-2 mechanism. Verified both of those specifically with a direct test (not just eyeballing): a synthetic trade scenario correctly found and ranked 3 real trade candidates, and the order-intent construction produced the exact right signal kind for the player's current node's resource type.

**Caught and fixed the HUD overflowing its reserved band again**, same category of mistake as before but caught before shipping this time: the new hatch status + keymap hint lines pushed HUD height to 208px, 8px past the existing 200px `TOP_MARGIN`. Measured with `pygame.font.size()` (not eyeballed) before finalizing, same discipline as the earlier fix, and this time actually caught it before showing the user instead of after.

**What's verified programmatically vs. what needs a human:** selftest, a headless smoke test (dummy SDL driver, 8s), and a longer stress test (30s wall time at 20 ticks/sec, ~600 ticks, ~30 lead-decision cycles per lead) all ran clean with zero crashes - that covers "does this break." Whether possession actually *feels* controllable, whether the numbered-neighbor labels are legible, whether two leads visibly pursuing different goals reads as intended on screen - all genuinely need the user's own eyes, same as Phase 1's rendering. Left live for that review.

## 2026-07-15 — Phase 2 stage 3 follow-up: the live view really was freezing/dying

User's report after actually running it: "crashing like hell." Checked the running process - it had died with no traceback anywhere (not in the app's own log, not in dmesg for an OOM/segfault). No exception means it almost certainly wasn't a bug raising an error - it means the process got killed from outside, which is exactly what happens when a desktop window manager decides an unresponsive app should be force-closed.

Root cause: lead decisions called Ollama **synchronously, inline in the main pygame loop**. A single call measured fast in isolation (~0.2s), but that's not the point - any slowdown (model needing to reload, the machine under load, Ollama contention from earlier test runs) freezes the *entire window* for its duration, with two leads potentially stacking that up to 30s+ if both stall. X11 marks a frozen app as not-responding, which reads as crashing to a user and can get force-killed by the desktop environment - consistent with what was actually observed (dead process, zero traceback). This was a known, named risk in the Phase 2 plan itself ("background-threading as a documented future improvement if the pauses prove annoying") - it proved annoying enough to actually break the app, so fixed it for real instead of leaving it as a someday-item.

**Fix:** lead decisions now run through a `concurrent.futures.ThreadPoolExecutor` (stdlib, no new dependency) instead of inline. A lead's decision is submitted as a background job when due; the main loop never waits on it - the lead just keeps its current safe-autopilot behavior (absent from `external_intents`) until the future resolves, whenever that happens to be, not pinned to an exact tick. `decide_lead_action` is read-only against world/agent state until its result is applied, so running it against live, concurrently-mutating state from a background thread doesn't risk corruption - worst case a decision is based on a slightly stale snapshot, which the existing resolve_* re-validation in `actions.py` already handles gracefully (same defensive pattern used since Phase 0's trade resolution).

**Verified the fix actually fixes it**, not just assumed: monkey-patched `leads.query_ollama` to sleep 10 seconds (simulating a fully hung Ollama backend) and ran `watch.py --quit-after 8`. Before this fix that would have blocked for 10+ seconds past the requested quit time; after the fix, it exited at 8.1s exactly as requested - the hang never touched the main loop at all. Re-ran the normal real-Ollama stress test (30s / 600 ticks) afterward to confirm nothing broke in the non-degraded case either.

## 2026-07-15 — Batch data analysis: 43 runs, 4 goals, one real surprise

Analyzed the four batches collected earlier today (20 seeds trade-on, 20 seeds trade-off paired on the same seeds, 3 seeds pushed to 40,000 ticks) with `analyze_batches.py`. Every number below was computed from the actual CSV/JSONL data and cross-checked at least once before being written down here - anything not independently verified is marked as such rather than stated as fact.

**Goal 1 — trade's effect, properly powered this time (n=20 paired, not n=1).** Specialization index: trade-on mean 0.494 (stdev 0.071), trade-off mean 0.374 (stdev 0.089), trade-on higher in 19/20 seeds. Consistent with the original single-seed Phase 0 finding, now on a real sample instead of one anecdote. **Genuine refinement worth keeping: trade's effect is on redistribution, not survival.** Population barely differs between conditions - trade-on mean 38.6/40, trade-off mean 39.1/40 (trade-off is if anything slightly *higher*). Trade reliably redistributes who holds what; it does not rescue a struggling population. The original LOG.md framing never explicitly claimed otherwise, but it's worth stating plainly since it would be easy to over-read "trade matters" as "trade helps agents survive," which this data doesn't support.

**Goal 2 — population survival distribution.** 19/20 seeds landed healthy (>=35/40 alive). Exactly one seed - seed 7, the one already flagged as a known hard case - fell into the moderate band (23/40). Zero seeds collapsed outright (<20/40). So seed 7 isn't representative of some hidden fragility across the world-generation process; it's a genuine, rare outlier among the seeds tested.

**Goal 3 — long-horizon (seeds 1, 7, 42 to 40,000 ticks), plus a free correctness check.** The determinism cross-check passed exactly for the two seeds that had a comparable short run: seed 1 and seed 7 both matched their independent 8,000-tick run bit-for-bit at the tick-8000 checkpoint (population, specialization index to full float precision, cumulative trade count) - real confirmation the simulation is genuinely deterministic given a seed, not just approximately so. (Seed 42 has no short-run counterpart in this batch, so that one specific check was skipped, not silently assumed.) Long-horizon trend: seed 1's specialization index kept climbing well past 8,000 ticks (0.427 -> 0.558 by tick 40,000), so the "plateaus early" read from the original Phase 0 analysis was itself an artifact of only testing to 3,000 ticks - over a longer horizon the economy keeps getting more specialized, it just does so slowly. Seed 7 stayed flat (0.528 -> 0.505, one more death by tick 40,000) - a hard world stays hard. Seed 42 actually drifted down (0.429 -> 0.386) while trade volume grew 4.5x (15,202 -> 69,472 cumulative trades) - worth flagging as genuinely unresolved: more trading activity co-occurring with a lower specialization index isn't intuitive and wasn't explained by anything checked in this pass.

**Goal 4 — the actual surprise.** Gather contention is not a seed-7 quirk. It's universal: fail rate across all 20 trade-on seeds ranges 69.3%-92.3%, mean 80.1% (stdev 6.8%) - independently recounted by hand for seed 7 to confirm the script (157,159 attempts, 12,118 succeeded, 145,041 failed, matched exactly) and spot-checked that "failed" entries really are no-ops (identical before/after state, empty delta). Contention starts almost immediately - by tick 16-17 in the sample checked, not something that builds up over time. This means the original framing in an earlier entry ("seed 7's remaining deaths are a different, more legitimate phenomenon... not a bug") undersold how common the underlying dynamic is - nearly every gather attempt in this simulation fails somewhere, in every seed, and most worlds shrug it off fine. **What actually separates seed 7 isn't contention rate** - seed 5 has almost the same fail rate (91.1%) and stays perfectly healthy (36/40). The real difference, verified directly: seed 7's *survivors* are thriving (avg hunger 9.4, energy 86.4) while seed 5's survivors are all moderately stressed (avg hunger 34.1, energy 56.5) despite losing almost nobody. That pattern is consistent with survivorship bias - seed 7 likely had an early die-off that then relieved pressure on whoever was left, while seed 5 spread the same pressure across everyone without anyone dying. **Flagged, not claimed:** why seed 7 specifically triggers that early die-off isn't re-diagnosed in this pass - an earlier entry attributed a different seed-7 death cluster to topological food isolation (fixed since), and that mechanism may or may not be what's driving this too. Worth a dedicated look before treating it as settled.

## 2026-07-15 — Gather contention fix: a near-miss, then a clean win

Went after the Goal 4 finding directly: agents had no sense of how many others were already competing for the same node, so they piled on uncoordinated and ~80% of all gather attempts failed. First attempt discounted the direct Gather score by how many other agents were already at that node (`colocated`, already available in `generate_candidates`) - cheap, used data already on hand, seemed obviously right.

**It wasn't.** Verified against 4 already-characterized seeds before trusting it (1, 5, 7, 9) and nearly reported it as a win on fail-rate alone - fail rate did drop, dramatically, in every seed. But population got *worse* in 3 of 4 (seed 1: 40->28, seed 9: 40->24, seed 7: 23->19) and cumulative trades collapsed almost everywhere (seed 1: 26,778 -> 1,817). Didn't report that as done - traced it first, and found the exact same failure shape as the earlier `SIGNAL_MOVE_BONUS` oscillation regression: one dying agent had shuttled back and forth between two nodes eight times in a row with zero food before starving. The direct Gather score at a crowded node was discounted, but the Move lookahead deciding whether to *travel* to a neighboring node had no idea that neighbor was equally crowded - so a congested node looked congested standing on it and falsely inviting from next door, producing the same "grass is greener" oscillation as before, just from a different scoring asymmetry.

**Fixed properly this time, matching the lookahead architecture instead of bolting onto one side of it:** replaced the `colocated`-based discount with a single `node_occupancy: dict[str, int]` snapshot (node id -> agent count), built once per tick, usable for *any* node - the agent's own location or a neighbor's. `_congestion_factor()` is now one function called identically from both the direct Gather candidate and `_best_local_score` (the Move lookahead), so a crowded node is discounted the same way regardless of whether an agent is standing on it or eyeing it from next door - no asymmetry left for an oscillation to exploit. Required threading `node_occupancy` through `generate_candidates`, `choose_action`, `leads.decide_lead_action`, and every caller (`tick.py`, `watch.py`'s two call sites, `test_leads.py`) - a real but mechanical signature change, verified complete with a project-wide grep for stale call sites before testing.

**Re-verified against the same 4 seeds, and this time checked for the oscillation signature directly, not just trusted the aggregate numbers:** fail rate still dropped meaningfully in all four (75.6%->42.2%, 91.1%->58.2%, 92.3%->18.3%, 69.3%->9.4% - notably gentler than the broken version's near-0% everywhere, which in hindsight was itself a warning sign of over-suppression, not a good result). **Population hit 40/40 in all four seeds, including seed 7** - the previously-collapsing world - going from 23/40 to a full recovery. Trade volume rose in 3 of 4 (seed 7: 3,046 -> 44,938 cumulative trades). Checked the most-traveled routes and most-moving agents afterward specifically to rule out a subtler oscillation: the busiest agent now moves in ~10% of ticks and the top route accounts for a normal share of traffic, nowhere near the 73%+-of-ticks-spent-moving, one-route-dominates-everything signature of the earlier regression.

**Not yet done:** this was checked against 4 seeds (1, 5, 7, 9), not the full 20-seed set the earlier batch analysis used - a real, positive result, but not yet given the same statistical weight as the paired trade-effect finding. Re-running the full sweep to confirm this holds broadly, and re-checking whether seed 7's die-off mechanism is now understood (it may simply have been the same contention this fix addresses) are natural next steps, not done in this pass.

## 2026-07-15 — Congestion fix confirmed at full scale (n=20, `analyze_congestion_fix.py`)

Re-ran all 20 seeds (same seeds, same 8,000 ticks as the original batch analysis) with the fix in place and compared directly against the existing `logs/batch` baseline - no more relying on a 4-seed sample.

**Population: 40/40 in every single one of the 20 seeds.** Mean before was 38.6 (stdev 3.9, only 15/20 at full population); after, mean is exactly 40.0 with stdev 0.0. Zero seeds got worse. Five improved outright, including **seed 7 fully recovering from 23/40 to 40/40** - the population collapse that's been tracked since Phase 0 is resolved, not just improved.

**Gather fail rate dropped in all 20/20 seeds**, mean 80.1% -> 28.0%, range now 5.1%-58.2% (was 69.3%-92.3%). Still real contention in places - this isn't claiming the dynamic is eliminated, just brought down to something much less wasteful.

**Cumulative trades rose in all 20/20 seeds**, nearly doubling on average (24,076 -> 48,718) - agents that spend less time failing to gather have more to trade with.

**Specialization index is a wash, not a win - worth saying plainly rather than folding it into the good news.** Mean is essentially unchanged (0.494 -> 0.490), and only 11/20 seeds improved; 9 got slightly worse. Plausible reading, not verified: with less contention, agents succeed at gathering their *own* specialty more reliably than before, which could reduce how much they still need to trade for it even as absolute trade volume rises - but that's a hypothesis from looking at the pattern, not something separately tested. Flagged, not claimed.

Net assessment: this is a real, broad fix for the thing it targeted (contention, population loss) with no population regressions anywhere across 20 seeds, and one metric (specialization) that neither improved nor degraded overall. Committing this as settled; the specialization-mix question and seed 7's original death-trigger mechanism remain open for later, not blocking.

## 2026-07-15 — Phase 3: the exporters

Built all three exporters DESIGN.md called for: `export_data.py`, `export_narrative.py`, `export_strategy.py`. One deliberate reinterpretation stated up front in the plan: the original doc described the strategy exporter as comparing "utility-weightings" across agents, which assumed per-agent weight variation that was never actually built (every crowd agent shares one scoring formula from `constants.py`). Redefined it instead as mining which *agents* did well and what distinguished them - a more honest fit for what the simulation actually is.

**`export_data.py`** — manifest + flattened CSV from a JSONL log. Verified against `logs/batch/seed1.jsonl`: exported exactly 343,038 entries (matches the line count independently confirmed during the batch analysis), git commit captured correctly, action counts (140,353 GATHER attempts) match the pre-fix numbers already on record exactly.

**`export_narrative.py`** — filters to lead-tier actions and deaths only, template-rendered to markdown. Caught one real bug before it shipped: assumed a `resource` field existed on log entries for SIGNAL actions; checked `actions.py`'s `resolve_signal` directly and found the signal kind (e.g. `"order:wood"`) is actually stored in the `target` field, not a separate one. Fixed before testing rather than after. Verified two ways: against a freshly generated log with real lead activity (produced a genuinely readable trade/craft/travel narrative, correctly showing `rich:food`/`rich:ore` signals), and against `seed7`'s known death cluster - exported exactly 17 death lines, matching the population drop (40->23) exactly, with the same tick numbers already recorded from the original hand-diagnosis (a33 at 1122, a36 at 1253, etc.).

**`export_strategy.py`** — single-run mode (top/bottom quartile by final wealth, compared by action mix) and compare mode (reusable version of the congestion-fix analysis). Real limitation stated plainly rather than worked around: agent `specialty` isn't part of the logged schema (`AgentState.snapshot()` never included it), so specialization index can't be recomputed from a JSONL log alone. Compare mode looks for the paired `_stats.csv` file (which `run.py` always writes alongside a log) and uses its already-computed value when present, and says so explicitly when it isn't rather than approximating specialty from gather frequency.

**Caught a real bug via the planned cross-check, exactly as it was supposed to work.** Compare mode's first version reported seed 7's trade count as 6,092 -> 89,876; `LOG.md`'s already-verified numbers from the congestion-fix analysis say 3,046 -> 44,938 - precisely double, in both directions. Traced it immediately: each successful trade swap produces two log entries (initiator + responder, per `actions.py`'s `resolve_trade_phase`), and the exporter was counting entries instead of swaps. Fixed, re-verified: all five metrics (population, trades, crafts, deaths, specialization index) now match the recorded numbers exactly. This is the free correctness check the plan called for, and it did exactly what it was supposed to - caught a real bug before anyone trusted the tool's output.

**Isolation confirmed**: grepped all three files' imports - stdlib only (`argparse`, `csv`, `json`, `os`, `subprocess`, `datetime`, `collections`). Zero imports from `tick.py`, `decide.py`, `actions.py`, or any simulation core file, matching the "no exporter touches the tick engine" constraint exactly.

## 2026-07-15 — Exporters validated at full scale, plus the one untested gap

After Phase 3 shipped, considered doing a full recheck of every phase given how much has accumulated - decided against it as mostly wasted effort, since Phase 0-2's core mechanics already have extensive hard evidence (43+ runs, a determinism check, multiple full-scale before/after comparisons) and re-running those again would confirm nothing new. Identified two specific, real gaps instead: the exporters had only been checked against 1-2 sample logs each, not the full 20-seed set everything else got; and `export_data.py`/`export_strategy.py` had never actually seen a log containing lead-tier entries (only `export_narrative.py` had, during its own build).

**Full 20-seed validation, not samples:**
- `export_data.py` against all 20 `logs/batch_post_fix` seeds: 0 crashes, and every single one's exported entry count matched its raw JSONL line count exactly.
- `export_strategy.py` compare mode against all 20 before/after seed pairs, cross-checked programmatically against the ground-truth `_stats.csv` files (not just eyeballed): population, cumulative trades, and specialization index matched exactly on **all 20/20 seeds** - the trade-counting fix from the Phase 3 build holds at full scale, not just for the one seed (7) it was originally caught and fixed against.
- `export_narrative.py` and `export_strategy.py` single mode against all 20 seeds: 0 crashes, and narrated death counts matched `40 - population` exactly for every seed (all zero, consistent with the congestion fix's confirmed 40/40-everywhere result).

**The lead-tier gap, closed:** generated a fresh log with real crowd + lead activity (20 crowd, 2 leads, 800 ticks, 1735 lead-tier entries) specifically to exercise `export_data.py` and `export_strategy.py` against tier variety they'd never encountered. Both handled it cleanly on the first try - `export_data.py` correctly reported `{"crowd": 20, "lead": 2}`, `export_strategy.py` correctly counted 22 total agents. No bugs found this pass, which is itself a useful, honest data point: the one real bug in this phase (trade double-counting) was already caught and fixed during the original build, not hiding in an untested corner.

Net effect: the exporters are now verified at the same scale and rigor as every other part of this project, not just Phase 3's original sample-level checks.

## 2026-07-15 — Interface catch-up: making the live view actually alive

User's framing, worth recording verbatim because it set the whole priority order for this pass: "remember the whole rick sanchez thing, if its not that visual whats the point then." Studying `render.py`/`watch.py` beforehand had confirmed the gap concretely - trades, deaths, crafts, and standing orders all happened with zero visual signal, even though they'd become central to everything learned in the last week of work. Also explicitly framed as a monitoring tool (user checks in, doesn't watch continuously), which shaped stages 4-5 specifically (recent history needs to survive a glance-away, not just reflect the instant you're looking).

Five stages, each smoke-tested before moving on, same discipline as every prior interface change:

**Stage 1 - foundation.** `stats.py` already computed trade volume, top routes, and active signal count every tick; `watch.py` never read any of it, and the fields were private besides. Added public accessors (`recent_top_routes()`, `route_traffic()`, `recent_trade_volume()`, `signals_active()`) matching the `cumulative_trades` precedent from Phase 2, plus a `reset_window()` watch.py actually calls - without it, `_window_routes` etc. would have accumulated for the entire session instead of reflecting recent activity, since only `run.py`'s headless `snapshot()` used to reset them.

**Stage 2 - the graph itself becomes visibly alive.** Edge thickness/color now scales with recent traffic (a real heatmap, not a static grid) and nodes carry a marker for whichever signal is active (`order:` > `scarce:` > `rich:` priority, so a lead's standing order - previously completely invisible - now visibly sits on the map for as long as it's actually active). Verified with real data, not just "doesn't crash": 21 distinct routes with genuine traffic variance (60-135), 12 of 15 nodes carrying live signals in a real test run.

**Stage 3 - transient effects for trade/death/craft, the biggest "alive" payoff, and where a real design flaw got caught before shipping.** Built an `Effect` system (trade = fading line pulse, death = expanding fading ring replacing the previous silent vanish, craft = a quick sparkle), driven off `run_tick`'s own returned entries with zero new simulation-side plumbing. First version rendered one effect per event unconditionally - verified against real post-fix data and found TRADE and CRAFT are the two *most common* successful actions in this simulation (roughly 1-in-4 and 1-in-5 of all successful actions, confirmed by direct count), meaning unthrottled effects would mean 150-210+ simultaneous flashes on screen, contradicting the plan's own "avoid visual noise" principle. Fixed with sampling (15% of trades/crafts get an effect, every death always does) plus a hard cap as a backstop - re-verified with the same realistic wall-clock-paced test and got a steady 25-42 simultaneous effects instead of 200+. Used a **separate, unseeded RNG for the sampling decision** specifically so a cosmetic choice never perturbs the simulation's own deterministic sequence - confirmed by grep that `effect_rng` never reaches `run_tick` or any simulation-state function.

**Stage 4 - event ticker.** Reuses `export_narrative.py`'s `describe()` directly (via `dataclasses.asdict()` on the tick entries) rather than duplicating its templates - the same lead-action-or-death filter, rendered as a scrolling feed so glancing away and back still tells you what happened. Verified with real output, not just structurally: 113 real narrative lines from a 50-tick test run.

**Stage 5 - trend sparklines.** Rolling history for population and specialization index, sampled on the same window-reset cadence as stage 1's accessors. Verified against seed 7 specifically (the historically hard case): population sparkline would correctly show a flat line at 40/40 (accurately reflecting the congestion fix holding), while specialization index shows real, meaningful fluctuation (0.41-0.56) worth actually seeing move, not just reading as a single static number.

**One measurement mistake caught mid-build, not after**: the event ticker's first width estimate (480px) was based on a hypothetical line that included goal/personality flavor text `describe()` doesn't actually generate - checked the real templates before trusting it, found the actual longest realistic line (a standing-order message) measures 522px, and resized before it could have clipped on screen.

All five stages verified individually, then together in an extended 30-second/600-tick stress test at the harder seed (7) with nothing crashing. Left live for the user's own review - same as every prior interface change, "does this feel alive" is a human judgment call no script can make.

## 2026-07-15 — Interface catch-up follow-up: the overlap bug, for real this time

User's report after looking: "things overlay on top of each others just like before." Right to push back - this was the third round of the same category of bug (HUD/legend overlapping the graph, twice, earlier this session), now recurring at the bottom with the new ticker/sparklines. Studied it properly this time instead of tuning another number and hoping.

**First measurement confirmed the obvious part**: `BOTTOM_MARGIN` was still 70px, while the ticker (178px) and sparklines (132px) needed far more room than that - the same margin-too-small mistake as before, straightforward to fix by increasing it.

**But increasing the margin alone didn't fix it, and understanding why exposed a real geometry bug, not just a sizing one.** The ticker/sparkline positioning formulas placed both boxes *above* the `h - BOTTOM_MARGIN` line - i.e., extending upward into the graph's own rendering rectangle - instead of *below* it, inside the reserved band `to_screen()` guarantees the graph will never enter. Backwards from how `TOP_MARGIN` correctly isolates the HUD and legend (both live entirely within `[0, TOP_MARGIN]`, outside the graph's rectangle). Increasing `BOTTOM_MARGIN` without fixing the direction just moved the collision zone around rather than closing it.

**Verified empirically instead of trusting formulas this time**, because the circular layout becomes a wide ellipse once mapped through the screen's very different width/height scaling, and reasoning about which angles are safe by hand had already gone wrong twice: computed real `to_screen()` positions for every node across 10/12/15/18/20/25/30-node worlds (the circular layout's angles depend only on node count, not seed, so this covers the realistic range in full) and checked each against all four UI boxes with `NODE_RADIUS_MAX` clearance. First real count: **270 collisions.** Fixed the geometry (boxes positioned inside the reserved band, not above it) - down to 65, all boundary grazes because the gap between the graph boundary and the box (10px) was still smaller than `NODE_RADIUS_MAX` (26px), so a node's visual edge could still reach past it. Widened the gap to 32px (exceeds the radius with margin) and re-ran the same 40-configuration check: **0 collisions.**

Re-ran the full verification suite (selftest, 30s/600-tick live stress test) after - clean. The actual lesson, worth keeping this time: a margin number being "big enough" isn't verifiable by eye or by one example screenshot - it's a real geometric claim about every node position a real world can produce, and this project already had the tool (computing `to_screen()` directly and checking) to verify it properly the first time, two overlaps ago.

## 2026-07-15 — v2 Phase 0: Rust core port, first parity check

Ported `world.py`/`agents.py`/`decide.py`/`actions.py`/`tick.py`/`log.py`/`stats.py`/`run.py` to Rust (`core-rs/`), module-for-module, same formulas and resolve order. Deliberately not porting Python's exact `random.Random` (Mersenne Twister) algorithm - too much incidental complexity for no real benefit here. Rust uses `rand_chacha::ChaCha8Rng` seeded from the CLI seed instead, so a given seed number produces a *different* actual world/RNG sequence than the same seed in Python. Parity is therefore checked statistically (same qualitative findings, comparable magnitudes), not via bit-identical logs - documented as a deliberate choice, not an oversight.

**Compiled clean on the first try**, selftest passed (50-tick bounds check, world connectivity).

**Ran the same 20-seed (1-20) paired trade-on/trade-off comparison from the congestion-fix analysis, on both engines, 8000 ticks/40 agents/15 nodes.** Python (current code) reproduced its own recorded numbers exactly - 48,718 mean cumulative trades, population 40.00/40 stdev 0.00 on trade-on - confirming the running Python state matches what's already been verified, not drifted. Rust: spec index mean 0.491 (trade-on) vs Python's 0.490 - same finding, trade demonstrably raises specialization (trade-on higher in 17/20 Rust seeds, 16/20 Python seeds - same magnitude of effect).

**Rust population: 19/20 seeds at 40/40, one (seed 19) declined to 24/40** - looked exactly like the pre-congestion-fix collapse shape (gradual decline, then plateau, not a cliff). Investigated instead of dismissing: re-ran Python itself on 53 seeds *outside* the originally-tested 1-20 range (21-73) and found the same phenomenon at a comparable rate - seed 45 dropped to 22/40, seed 71 to 26/40, seed 33 to 34/40, seed 56 to 36/40, roughly 1 in 10-13 seeds. Confirms this is a genuine, pre-existing property of the mechanic (DESIGN.md already documents the congestion fix as reducing contention, "not eliminating the dynamic"), present in both engines at a similar rate - not a Rust translation bug. The original 20-seed Python test set simply hadn't happened to include one of these harder topologies; a wider sample shows they exist for Python too.

**Real infra snag, not code**: the sandbox's root disk was at 99% capacity (5.4GB free of 468GB) system-wide, unrelated to this work but surfaced by it - the raw per-tick JSONL logs from parallel 8000-tick batch runs (tens of MB each x 80 runs) filled the remaining space mid-batch, corrupting a few in-flight stats CSVs (silently truncated, not wrong data - caught by checking row counts before trusting any number, re-ran the affected runs clean). Logs weren't actually needed for this check (only the stats CSV summaries were) - routed them to `/dev/null` on the redo and the problem didn't recur.

Phase 0's proof condition from `DESIGN-V2.md` is met: the Rust core reproduces the Python core's proven findings at comparable magnitude, on the same test protocol. Phase 1 (service/viewer split) is next.

## 2026-07-15 — v2 Phase 1: persistent service, live viewer, real disconnect/reconnect proof

Restructured `core-rs` into a library (`seam_core`) plus two binaries: `run` (the existing Phase 0 headless CLI, unchanged) and a new `serve` (`src/serve_main.rs`) - the persistent simulation service. `serve` owns the world behind a `Mutex`, ticks it on its own async loop (`tokio`) independent of any connection, and exposes it two ways: `GET /state` (one-shot REST snapshot) and `GET /ws` (live push over WebSocket, broadcast to any number of connected viewers). A static HTML/JS page at `/` is the viewer - connects on load, shows tick/population/energy/hunger/trades/specialization live, with explicit connect/disconnect buttons.

**Two scope calls made deliberately, not defaulted into:**
- **Viewer is plain HTML/JS, not React.** `DESIGN-V2.md` names React as the eventual target, but Phase 1's actual proof (world persists independent of viewers) doesn't need it, and a full npm/Vite toolchain risked a real, unpredictable disk hit while the disk was still sitting at 15GB free after Phase 0's near-miss. Zero new footprint this way. React is deferred to whenever a real UI need (not just "the design doc said so") justifies pulling in the toolchain.
- **No Python sidecar yet.** Also named in `DESIGN-V2.md`, but with no LLM logic to orchestrate until Phase 2's leads exist, a pass-through sidecar right now would be scaffolding with no job - added when Phase 2 actually needs it, not before.

**A real borrow-checker snag, not a logic bug**: `run_tick(tick, &mut sim.world, &mut sim.agents, &mut sim.rng, ...)` failed to compile when `sim` was a `MutexGuard` - Rust couldn't see the three field-borrows as disjoint through the guard's `DerefMut`. Fixed by materializing one explicit reborrow (`let sim: &mut SimState = &mut guard;`) before the field accesses, which gave the compiler a plain struct to split fields on. Compiled clean after.

## 2026-07-15 — v2 Phase 2: leads get memory + identity, verified with a real on/off experiment

Checked how v1 actually avoided blocking on Ollama before designing anything (`leads.py` + `watch.py`): a `ThreadPoolExecutor` runs the LLM call off the main loop, and a lead with no finished decision this tick just runs the crowd's own scoring - the fallback isn't a separate code path, it's just "absence from the override map." Reproduced the same shape across the process boundary this phase introduces (Rust service + a new Python sidecar), rather than inventing something new.

**Rust side:** brought back an `external_intents` override in `run_tick` (dropped in the Phase 0 port since headless didn't need it) - a lead with a fresh decision posted uses it for exactly one tick, otherwise it runs on crowd autopilot, unchanged. Added `spawn_leads` (3 leads, same goals/personalities as v1: wealthiest-trader, keep-stocked, out-produce). New endpoints: `GET /leads`, `GET /leads/:id/candidates` (the numbered legal-option list, same shape as v1's prompt), `POST /leads/:id/intent` (the sidecar echoes back the *actual* Intent object it got from `/candidates`, not just an index - avoids any staleness mismatch from however long the Ollama call took), `POST /leads/:id/memory`, `GET /agents` + `POST /agents/identities` for crowd naming.

**A design refinement made while building, not in the original sketch**: split lead memory into two genuinely independent halves instead of one LLM-dependent one. `caution_bias` (the number that actually discounts a lead's TRADE candidate scores, in the exact same `generate_candidates` function crowd and leads share) is now computed *mechanically* every tick straight from the free counters (recent trade success ratio, hunger scares witnessed) - no LLM involved at all. `memory_summary` (the first-person text) stays the LLM's job, fed into future prompts as context and read by a human in the viewer. Reasoning: asking a 3B model to output a calibrated float alongside prose is exactly the "small models are unreliable at structured output" trap this project already avoids elsewhere; splitting the mechanical number from the narrative text sidesteps it entirely, and it means memory still shifts behavior even during the (correct, expected) ticks the LLM hasn't answered on.

**Python sidecar (`sidecar/sidecar.py`)**: reuses v1's exact `query_ollama`/`_parse_choice` logic, adapted to work over HTTP against the Rust service. A `ThreadPoolExecutor` handles per-lead decisions and periodic self-summaries the same non-blocking way v1 did; a model that fails to answer usefully just means nothing gets posted, and the lead runs on autopilot - the fallback guarantee falls out of the architecture for free, it didn't need its own code path. Also does the crowd's one-time batched naming at startup (permissive line-based parsing, not JSON - a partial batch of names is fine, this is cosmetic).

**Verified end-to-end with a live model, not just described**: ran `serve` + `sidecar.py` against real Ollama (`llama3.2:3b`, already on the machine). Watched real, distinct self-summaries land - lead0's mentioned "a detour into the food market" after it actually had a hunger scare; lead2's reflected its real 100% trade success rate and matched its competitive goal. Confirmed the override pathway with a manual test too: posted a `MOVE -> n3` intent by hand and watched the lead's location change exactly one tick later.

**The actual proof condition - a controlled, reproducible experiment, not a live demo.** Live Ollama timing isn't something you can build a rigorous statistical comparison on, so added a `memory_enabled` toggle to `run_tick` (gates only the mechanical `caution_bias` recompute) and `--with-leads`/`--no-memory` flags to the headless `run` binary, letting the memory mechanism be isolated and tested deterministically with no sidecar or model involved. Ran 15 seeds x 2 conditions (memory on/off), 3000 ticks, 3 leads each. Found 13 real hunger-scare episodes per condition (first time a lead's hunger crosses 60) and compared trade-action frequency in the 100 ticks before vs. after.

**Honest result, not the simple story**: trade activity actually *increases* after a hunger scare in both conditions (a hungry lead needs to trade for food) - memory ON shows a smaller increase (mean +19.3 percentage points) than memory OFF (mean +22.2 points). Memory measurably dampens the post-scare jump in trading rather than suppressing trade outright - a real, modest, directionally consistent effect attributable specifically to the memory mechanism (the only thing toggled between conditions), not the cleaner "cautious leads trade less" headline originally expected. Worth stating plainly rather than rounding up to a bigger claim than the data supports.

Phase 2's proof condition from `DESIGN-V2.md` is met. Phase 3 (hatch: steward) is next.

## 2026-07-16 — v2 Phase 3: the hatch, a settlement, and a real neglect-vs-tend result

A machine reboot (the user was mid-tinkering with lid-close power settings) killed the background `serve`/`sidecar` data-collection pair between sessions - confirmed nothing was actually lost first: git was clean, the accumulated JSONL logs survived on disk (only the live processes died, as expected from an actual reboot, not a crash), Ollama came back up on its own. Restarted the pair with a fresh log file rather than appending to the old one, so two different simulation runs' tick numbers never collide in one file.

**Settlement**: no new mechanic, just a label - one designated node (`world.node_order[0]`) plus a fixed roster of 8 crowd agents relocated there at spawn, so the player is never handed an empty settlement by an unlucky seed's random scatter. Health is shown as the raw numbers discussed and agreed beforehand, not a composite score: population alive, average hunger/energy of the roster, and total food currently held between them.

**The hatch**: a new agent, mechanically identical to a lead (same `generate_candidates()`, same numbered-options pattern already built for leads in Phase 2) - the only difference is nothing ever drives it automatically. Reused the exact same `pending_intents` map leads already use for their LLM-sourced overrides (renamed from `pending_lead_intents` since it now serves both), just fed from a new `POST /player/action` instead of the sidecar. Standing orders needed zero new code - the hatch issuing a `SIGNAL` with `order:food` is the identical mechanic crowd/leads already respect from Phase 0.

**Viewer**: a settlement panel (the raw numbers) and a hatch panel that polls `GET /player/candidates` every few seconds and renders them as clickable buttons - direct possession, no LLM in this path at all.

**The verification took two attempts, and the first result taught something real.** First playtest used an arbitrary seed/world that turned out to be an "easy" one - the settlement roster thrived regardless of whether the hatch helped, so neglected and tended looked the same. Recognized this wasn't a real test and switched to seed 19 at 40 agents/15 nodes - the exact hard case Phase 0 had already found produces genuine population stress in this engine, so there was actually something for neglect to fail against and tending to protect.

**Even then, the first comparison (endpoints only) looked unconvincing** - both conditions ended at the same final population. Looking at the full trajectory instead of just start/end told a different story: neglected reached its floor (population 5) by tick ~1000 and stayed there; a first-pass tending script (individual food gifts, low-priority standing orders, polled only every ~33 ticks) showed some benefit but not much. Two real problems found and fixed by reasoning about the actual mechanic rather than re-running blindly: the standing-order signal expires in 30 ticks (`SIGNAL_TTL`) and was being refreshed only every ~33 ticks - not sustained - and the heuristic was reaching for it last instead of first, when it's the systemic lever (biases the whole roster's own gathering) rather than the one-off individual trade.

**Re-run with the order-first, tighter-cadence script**: neglected reaches population 5 by tick ~1028 and stays there for the rest of the run. Tended holds full population (8) until tick ~803, then declines much more gradually - staying at population 6 for roughly 2,000 ticks - before finally reaching population 5 around tick ~3749, over 3.5x longer than neglected took to hit the same number. Real, substantial, and directly attributable to whether the settlement was tended (same seed, same starting state, tending is the only variable) - not literal population growth beyond the start, more precisely characterized as active stewardship measurably delaying and softening decline. Reported at that size rather than rounded up to "recovery."

Phase 3's proof condition from `DESIGN-V2.md` is met. Phase 4 (narrative generation) is next.

## 2026-07-16 — v2 Phase 4: narrative generation, and a real hallucination caught before it shipped

Checked v1's existing `export_narrative.py` before writing a single prompt, since there was already a proven "voice" worth keeping instead of inventing a new one: template-based, terse, present-tense, factual ("Tick N — agent trades wood for ore with lead0"), deliberately excluding routine crowd noise so the rare lead/death events don't get buried. That restraint - no invented color, no melodrama - carried straight into how the LLM should write too, matching the user's standing preference against "AI narration mode" fluff.

**Rust side**: a capped rolling feed (`narrative_feed`, last 20 entries) added to `SimState`, purely additive - nothing about it can affect the simulation, it's read-only narration layered on top. `GET /narrative` and `POST /narrative` (the sidecar posts a scene, gets tagged with the current tick automatically). Folded into the existing `Snapshot`/WS push alongside leads/hatch/settlement, so the viewer needed no new polling mechanism, just a new panel.

**Sidecar side**: a third periodic task (`write_narrative_scene`) alongside lead decisions and memory summaries, same non-blocking pattern, on a slower cadence (default 90s - a scene now and then, not a play-by-play). Feeds the previous scene back in as context for continuity between successive scenes.

**The first real test caught a genuine quality problem, not a code bug.** The first prompt asked the model to "narrate a scene" - and it did, inventing a woman gathering berries, an elderly man sharpening an axe, a hunting expedition, none of it grounded in anything the sim actually tracks (there's no berries, no axes, no hunting in this economy - only ore/food/wood and the seven actions). Worse: because the prompt fed the *previous* scene back in as continuity, this fictional thread kept compounding on itself across several cycles, eventually inventing a named character ("Emma") and dialogue that never happened. Continuity amplified a bad first scene rather than staying neutral.

**Fixed by reframing the ask, not just adding "don't hallucinate."** Reworded the prompt from "narrate a scene" to "write a two-sentence status report using ONLY the facts given," explicitly ruling out inventing people, objects, or actions not in the data, and ending the prompt with "Status report:" instead of an open invitation to write prose. Re-tested from a genuinely clean run (fresh `serve` + fresh `sidecar`, no poisoned continuity): every scene came back grounded in real numbers - a lead's actual energy/hunger, the settlement's actual population and food count. One minor remaining rough edge worth noting rather than hiding: the small model (`llama3.2:3b`) occasionally confuses an agent's *location* field with "wealth" ("Lead0's wealth remains at n0") - awkward phrasing, not fabrication, a much smaller class of error than inventing whole characters.

**This phase's proof condition is a human judgment call by design** - `DESIGN-V2.md` says as much, the same honest caveat v1 gave its own first live-feel pass. The mechanism is built, verified end-to-end, and caught a real failure mode before it reached the user rather than shipping invented narrative as if it were reporting on the actual world - but whether the writing is actually good enough to want to read is not something I can verify myself; that's for a human to look at the live feed and judge.

## 2026-07-16 — v2 Phase 5 (final phase): the data layer, and a real toolchain fix along the way

Checked what was actually available before assuming anything needed installing: Postgres 16 (plus `psycopg2`) was already on this machine for other projects, reachable over the local unix socket with no password needed - zero new setup required, just a new database (`seam`) and a lean four-table schema (`core-rs/schema.sql`): `events` (the same lead/death entries JSONL already captures, plus `specialty` - closing a real, explicitly-documented v1 gap: `agents.py`'s `snapshot()` never logged it, so `export_strategy.py`'s compare mode had to fall back to a paired `_stats.csv` file for the specialization index), `lead_memory_snapshots`, `narrative_scenes`, and `settlement_snapshots`.

**A real toolchain problem surfaced and got fixed properly, not worked around.** Adding `tokio-postgres` pulled in a dependency chain requiring Cargo's `edition2024` feature, which the machine's apt-installed Rust 1.75 (from Phase 0, chosen because rustup's download was too slow back then) can't support. Spent a while trying to pin individual transitive dependency versions down (`sha2`, `block-buffer`, `postgres-protocol`) before stepping back and checking whether the actual constraint - slow network - still applied. It didn't: a live speed check showed ~1.5MB/s, up from the ~40KB/s that made rustup impractical in Phase 0. `rustup default stable` finished in seconds and installed 1.97.0, fixing the edition2024 problem outright and, as a bonus, fixing the broken `~/.cargo/bin/cargo` rustup shim that's been requiring a `PATH` workaround since Phase 0. Confirmed no behavior regression with the existing selftest before going further.

**Rust side**: `serve` gets an optional `--postgres-url`/`--run-id`, additive alongside the existing JSONL/CSV logging (not a replacement - no reason to break what already works). Writes happen after the sim's `Mutex` guard is released, not across the `.await` - rows are extracted into plain owned structs while the lock is held, then the actual database calls happen once it's free, same discipline as the broadcast send already used.

**Exporters**: all three gained a `--postgres-url`/`--run-id` mode alongside their existing `--log-path` mode, verified to produce identical output to before in JSONL mode (byte-for-byte exact on `export_narrative.py`'s death-count, unchanged action-mix numbers on `export_strategy.py`) and real, working output against a live Postgres-backed run. `export_narrative.py` got a genuine upgrade, not just a new input format: Phase 4's real LLM-authored scenes now merge into the timeline at their actual tick position alongside the templated event lines, clearly set off with a blockquote. Watched it happen for real: a scene reading "Lead0 is the wealthiest trader in the region with 75 units of energy..." landed correctly between two trade events at tick 156, grounded in the same numbers the templated lines showed. `export_strategy.py` now computes specialization index directly from logged specialty when available, instead of needing the paired-CSV workaround - the documented v1 limitation actually closed, not routed around.

**Proof condition from `DESIGN-V2.md` met**: all three exporters ran against a real v2 Postgres-backed run and produced output at least as useful as their v1 equivalents, with genuinely new data available (lead memory over time, settlement health over time, real narrative merged into the story) that v1 never had access to.

This is the last of the six planned phases. `v1.0.0`'s crowd-and-trade mechanic, proven and shipped, now has a Rust core, a persistent service anyone can watch, LLM-driven leads with real memory, a player-stewarded settlement, real narrative generation, and a proper data layer underneath all of it - each phase built to a concrete proof condition, not "feels done," and each real gap or surprising result reported at the size it actually was rather than rounded up.

## 2026-07-16 — Pivot decision: Watchable World, and two sibling explorations kept separate on purpose

With v2 complete and a real background collector accumulating live data, the conversation turned to seam's dual-use potential - honestly, not defensively: what is this project's tech actually made of once the "simulated economy" flavor is stripped away, and where could that same underlying mechanic point if aimed somewhere else? Three pivots came out of that discussion.

**Watchable World** is the one that's actually recommended, and it goes into seam itself (`V3.md`, the carry-forward sketch - `DESIGN-V3.md` comes once real planning happens, same pattern as `V2.md` before it). Not chosen by default or for safety - chosen because it's what the user's own scattered notes, written down as they occurred ("seam -> self sustainable world, make seam into a watchable world?, its pretty fun man its hilarious, group the agents into societies with leaders"), were already pointing at, and genuine unprompted enjoyment watching the leads and a hard-seed settlement's real collapse is data worth taking seriously. The concrete mechanic: generalizing from one settlement to many, each with its own roster and leadership - the single piece of work that, as a side effect, is also the shared foundation both sibling pivots below need.

**Two other pivots exist as separate repos, deliberately not folded into seam**: `seam-logistics` - the same mechanic retargeted at real supply-chain/resource-allocation planning, straightforwardly commercial. `seam-swarm` - the same lead/standing-order structure re-themed as swarm/unit command-and-control, the closest structural match to the drone-tracking-to-military-use example that prompted this whole exercise. Both are exploration-only right now - a README and a `PIVOT-NOTES.md` capturing the reasoning, nothing built. `seam-swarm`'s notes carry an explicit, deliberately-written boundary: simulation and data only, no real-world actuation, no real hardware or targeting, and if that line ever needed to move, that's a fundamentally different category of work requiring real authorization this project doesn't have.

One finding from the exact data-science pass that prompted this conversation is worth keeping in mind going forward, not just noting once: the structural-bottleneck analysis (does reducing resource density cause collapse, does stewardship measurably delay it) is genuinely symmetric - the same number that says how much intervention prevents a collapse says, with equal precision, how to engineer one. That symmetry doesn't go away because any of this stays a simulation.

## 2026-07-16 — A real critical pass on the accumulated data: two conclusions corrected, two open items closed for real

With six runs now accumulated (three easy-seed, three deliberately varied - a harder seed, more resource nodes at that same hard seed, and a fresh alternate seed), went back over the earlier findings specifically trying to break them rather than restate them with more confidence. Two held up and got more precise; two didn't hold up and needed correcting; two "still open" items got fixed instead of just re-flagged.

**Held up, and now more precise than before.** The "structural bottleneck" claim from the node-density comparison wasn't wrong, but it was vague. Checked directly: average agents piling onto the same node per gather attempt is 9.3 in the 15-node world vs. 1.57 in the 25-node world at the same seed - nearly 6x. That's the actual mechanism (the game's own congestion penalty triggering constantly vs. almost never), not just "seems harder with fewer nodes."

**Corrected: "top performers trade more" is not a general law.** Checked all four stress levels side by side instead of trusting the one comparison that produced it. Trade-driven wealth only wins at moderate scarcity (the original easy-seed run and the fresh alternate seed, both ~38-44% gather-fail rate). At severe scarcity it disappears - too stressed to bother trading. At the 25-node world's near-abundance (10.7% fail rate, the *lowest* of any run) it also disappears, for the opposite reason: wealthy agents there gather more and trade less, because self-sufficient production beats needing a trading partner when resources are easy to get. It's an inverted-U across scarcity levels, not a straight line. Said too generally the first time.

**Corrected: "goal drives lead behavior consistently" doesn't generalize.** The earlier "replication across 3 runs" (mainrun5/6/7) was the same seed and config restarted three times, not independent evidence - of course it looked consistent, it was close to the same deterministic dynamics each time. Testing genuinely different seeds/topologies, the pattern breaks: a different lead dominates in each of the three new runs, and in the 25-node world all three leads show nearly identical caution levels with no differentiation at all. The mechanic is real within one world, but isn't a property of the mechanic in general - it's topology-dependent, and claiming otherwise from same-config reruns was the actual mistake, worth remembering for any future "is this robust" question: rerunning the same setup is not replication.

**Two open items closed for real, not just re-flagged:**

*Narrative garbling, found by reading ~400 real scenes instead of a handful.* Two related bugs: the "if nothing changed, say so" instruction was producing confused, self-contradictory sentences ("currently located at node n2, but not at that place; according to the previous statement, they are now at node n2") when the model tried to explicitly contrast current vs. previous state, and a clarifying aside in the prompt ("a place, not an amount") was leaking verbatim into the output text next to the value it was meant to clarify. Fixed both: moved the node-id clarification out of the per-lead line into the general instructions (stated once, not repeated - it had already held for hundreds of scenes on its own), and reframed the continuity block as explicit background tone ("not to be quoted or compared against") instead of quietly implying comparison was expected while separately instructing against it. Re-tested live before rolling out: no garbling, no leaked asides, correctly grounded. Restarted all three sidecars to apply it - none of the `serve` processes touched, all three simulations kept ticking uninterrupted through the change.

*The LLM fallback path had never once fired in ~11,000 real decisions - reliable, but unvalidated.* Forced it directly rather than waiting for it to happen organically: pointed `query_ollama` at an unreachable URL and confirmed it returns `None` cleanly (not an exception), then called `decide_for_lead` and `summarize_for_lead` against that forced failure and confirmed both survive without crashing and post nothing - the lead genuinely falls back to autopilot for that tick, observed directly instead of assumed from the architecture.

## 2026-07-20 — External research pass, two decisions locked: relationships and an asymmetric-power campaign

With seam sitting at a stable point (v3 built, the n13 congestion bug fixed and documented in `ANALYSIS.md`, nothing currently running), did a deliberate outside-research pass before writing any more code - checking what's happening elsewhere that touches the same primitives, so the next architecture gets locked in with real context instead of in isolation. Covered five threads: emergent-agent-society sims (Stanford's Generative Agents, Altera's Project Sid/PIANO architecture, DeepMind's Concordia), multi-agent economic simulation (EconAgent/ABIDES-Economist/Market-Bench), hierarchical commander/follower LLM architectures, LLM-controlled swarm robotics (SwarmChat, CommandSwarm), and the real-world military C2/wargaming landscape (DoD Thunderforge, NATO, Army CGSC, the live "meaningful human control" regulatory debate).

**Two things from that pass are locked as real decisions, not just reading notes.**

1. Project Sid's own most-viral finding - two Gemini agents forming a relationship, getting frustrated with their town's governance, and burning it down - didn't come from more compute or richer stats. It came from agents holding real memory of *each other*, not just self-summaries of their own state. Seam doesn't have that: leads reason in isolation, crowd agents don't communicate at all, standing orders are one-directional. Identified as the actual missing piece behind the Watchable World ambition, ahead of population size or narrative polish. **Locked as Phase 5 in `DESIGN-V3.md`: a real relationship/memory-of-others subsystem** - built at the depth the ambition deserves, not a bolt-on.

2. A 2026 paper ("Bosses, Kings, and the Commons," the SOVSIM framework) found that introducing one asymmetric-power agent into a population of otherwise-symmetric resource-extracting agents caused up to 87% degradation in cooperation/survival, across eleven models tested. That's structurally close to seam's own lead/hatch-over-crowd design. **Locked as a dedicated research campaign** (documented in `DESIGN-V3.md`): a proper controlled experiment - multiple asymmetry levels, not just on/off, multiple seeds, the same rigor as the original 20-seed trade-on/off study - to find out whether seam's own stewardship result (tended settlement surviving 3.5x longer than neglected) holds because the design sidesteps this trap, or is quietly paying a cooperation cost elsewhere nobody has looked for yet. Note for whoever runs this: the raw per-decision data from the three long July runs was deleted during the 2026-07-19 disk near-miss (small `stats.csv` files survived, the raw logs didn't) - this campaign needs fresh, purpose-built runs, not a re-check of what's already on disk.

**Kept as reference, not promoted to a decision:** DeepMind's Concordia (Game Master pattern, Entity-Component architecture) is worth a skim before Phase 5's data structures get designed, but its per-action LLM routing is a worse cost/scale tradeoff than seam's own crowd/lead split - not something to converge toward, seam already made the right call there. Separately, the hierarchical-team literature's finding that a lead agent issues upward of 60% of a team's communication as one-way orders matches seam's current standing-order mechanic exactly - Phase 5's relationship work is the natural place to also add a feedback channel (crowd/leads signaling upward, not just receiving orders down), rather than spinning that off as its own future phase.

`seam-swarm`'s own slice of this same research pass (CommandSwarm's propose-validate-execute pattern, a multi-echelon command-depth direction) is documented separately in that repo's own `PIVOT-NOTES.md` - swarm stays unbuilt for now, this was a documentation pass to avoid losing the thinking, not a resumption of that pivot.

## 2026-07-20 — Workstream A: narrative generation was silently dead since v3 Phase 0, fixed and verified live

A codebase exploration pass ahead of Phase 5 turned up a real, previously-uncaught regression: `sidecar.py`'s `write_narrative_scene` called `client.get_settlement()`, which hit `/settlement` - a route Phase 0 (2026-07-16) removed in favor of `/societies` when the single-settlement mechanic generalized to N societies. `get_settlement()` had been returning `None` on every call since, so `write_narrative_scene` bailed out silently every single cycle - narrative generation has produced nothing for four days without anyone noticing, since nothing about it errors loudly.

**Fixed properly, not patched around**: `ServiceClient.get_settlement()` replaced with `get_societies()` (`/societies`, returns the list `build_society_view` already produces per society - same field names the old single-settlement dict used, just N of them instead of one). `build_narrative_prompt` and `_narrative_signature` changed from taking one `settlement: dict` to `societies: list[dict]`, looping to describe every society instead of assuming exactly one exists.

**Verified live, not just compiled clean**: ran a real two-society `serve` instance plus `sidecar.py` against actual Ollama (`llama3.2:3b`), narrative interval shortened to 5s for the test. `GET /narrative` came back with three real, tick-tagged scenes correctly describing both societies by id with their actual population/hunger/food numbers, and referencing leads by their real assigned names and one lead's real `memory_summary` quote - not a crash, not an empty list, not fabricated content. Test `serve`/sidecar processes killed after confirming, nothing left running.

